import { ArticlesField } from "../fields/ContentFields";

// Keep older imports on the same graded, private-message-only experience.
export function AdultPage() { return <ArticlesField kind="adult" />; }
