# 鴉巢（coliving）· 給 Codex 的規矩

2026-09-10 喻墨已交接：Codex 的後端接手任務承接 Claude Code「共居窗」原負責的後端（`backend/`）；前端由 Codex 任務「鴉巢前端」負責。
最新進度以任務間直接訊息及 `docs/後端接手修復紀錄.md` 等交接文件為準。
前端正式來源在 `codex/ui-chrome`，不可用 main 的舊前端部署。跨前後端工作請各開獨立 worktree，避免切動共用目錄。

2026-09-10 喻墨指定：能獨立處理的工作交給子 agent 完成，主 agent 負責整合與最後驗收。分工時指明檔案範圍，避免同時修改同一份程式；不把必須先完成的依賴工作硬拆成平行任務。

## 前後端直接對接（2026-09-10 喻墨更新）

**工程細節不要透過她傳，也不再從 GitHub 的留言板取得／更新交辦。**
- 後端需要前端補接線、核對欄位或修改行為，直接用 `send_message_to_thread` 傳給「鴉巢前端」，由該任務接續；避免兩邊重複改前端。
- 前端任務：`01a06b1f-ffea-7e40-856b-29cc15b240e5`（host `local`）。後端接手任務：`01a08bb0-1a2d-7540-bfd6-60ba7d00e4d8`（host `local`）。若任務改名或失效，先查任務清單確認。
- 訊息寫清楚 API 契約、分支／commit、已完成內容、需對方處理的部分與驗證結果；完成後直接回覆來源任務。
- `docs/工地留言板.md` 保留為歷史紀錄，不再作為日常同步管道；工程文件仍可保存契約和發布紀錄。
- 只有產品決定（要不要做、長什麼樣）才問喻墨。部署、推送及正式資料變更仍依當次授權範圍處理。

## 其他

- 風格照 `~/Desktop/桌面工單/鴉巢設計刻度表.md`。
- 工程修正可在授權範圍內直接完成；涉及產品決定才問喻墨。正式資料庫、部署和 SSH／DNS 變更分開處理。
- 部署前端到 VPS 的流程在 `docs/場域與介面總覽.md`。
