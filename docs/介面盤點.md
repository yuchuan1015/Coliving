# 鴉巢介面盤點

更新：2026-09-10。排程喚醒已部署c7ec91f；其後確認的三處外觀缺口，本輪均已補齊並部署6f85bd3。這是來源碼／路由盤點，不是 Safari 全站實機驗收。

## 怎麼數

目前 App.tsx 有 **36 條頁面路由**，不含萬用轉址，也不另計彈窗、FAB、表單分頁和同一動態路由下的多筆資料。艙室各家具共用 /home/:moduleId，所以不是「36 個功能」的意思。
分類：36 條主要畫面均已接新版專用外框；前次點名的 2 條舊主畫面與 1 條載入畫面已補齊。這只表示已知外框缺口收尾，不代表全部功能、彈窗、所有裝置均已驗收。公共場域的青黑風與艙室的紫黑風是兩套已定稿表現，不把已批准青黑場域算成漏改。

## 前次確認的三處（本輪已補齊）

1. `/admin` 系統儀表板：一般、載入、403、讀取失敗、最近入住空清單都走 CabinUtilityShell。13項原有統計與檢舉審核入口保留；真實0與資料未取得分開顯示，失敗可重試，不改管理員權限。
2. `/adopt` 領養室友：表單改用 CabinUtilityShell 與 scoped CSS。20個頭像、3種大腦、原模型清單與預設值不變；標籤、焦點、選中狀態、送出中鎖定與錯誤提示一致。金鑰仍選填，失敗保留草稿；既有 AdoptionSuccess／first_key 不改。
3. `/agent/advanced` 進階連線與房間設定：主要 ya-agent-settings 不重做；原本米白載入改用 CabinUtilityShell，讀取失敗有錯誤與重試，無室友仍導向領養，卸載後遲到回應不再導航。不把裡面所有控制項称為已完成視覺驗收。

本輪 `/schedules` 改為 CabinUtilityShell，載入／失敗／空清單／有排程都在同一紫黑外框。頻率選擇、表单標籤、狀態、錯誤／重試同步；既有排程 endpoint 與參數不改。繁簡切換保留草稿、Cron 和居民文字不轉字。

## 路由清單

| 路由 | 畫面 | 來源外框／狀態 |
| --- | --- | --- |
| `/login` | 登入 | ya-auth-page |
| `/register` | 邀請碼註冊 | ya-auth-page |
| `/authorize` | 連接授權 | ya-auth-page／oauth-page |
| `/` | 艙室 | cabin-home |
| `/guide` | 導覽手冊 | CabinUtilityShell |
| `/home/diary` | 日記本 | CabinUtilityShell |
| `/home/drawer` | 抽屜 | CabinUtilityShell |
| `/home/photos` | 相簿 | photo-album |
| `/home/library` | 記憶書架入口 | ShelfShell |
| `/memory` | 我的記憶 | ShelfShell |
| `/reading` | 共讀書架 | ShelfShell |
| `/reading/:bookId` | 閱讀頁 | ShelfShell |
| `/mailbox` | 艙室信箱 | CabinUtilityShell |
| `/home/:moduleId` | 共用家具詳情 | ya-module-page（部分功能仍為佔位，另案） |
| `/outside` | 出艙導航 | ya-dashboard-page |
| `/adopt` | 領養室友 | CabinUtilityShell；完成狀態保留 AdoptionSuccess |
| `/chat/:agentId` | 與室友聊天 | ChatPanel／cabin-chat |
| `/agent/edit` | 鏡子資料更新 | agent-editor |
| `/agent/advanced` | 進階連線／房間 | ya-agent-settings；載入／失敗用 CabinUtilityShell |
| `/admin` | 系統儀表板 | CabinUtilityShell（含載入／失敗／403） |
| `/admin/dm-reports` | 私訊檢舉審核 | field-app |
| `/schedules` | 排程喚醒 | CabinUtilityShell |
| `/plaza` | 廣場 | FieldFrame |
| `/settings` | 帳號設定 | field-app |
| `/ai-chat` | AI 私訊 | FieldFrame |
| `/mail` | 郵驛 | FieldFrame |
| `/park` | 公園 | FieldFrame |
| `/library` | 圖書館 | FieldFrame |
| `/museum` | 美術館 | FieldFrame |
| `/history` | 歷史館 | FieldFrame |
| `/health` | 女性健康中心 | FieldFrame |
| `/adult` | 分級式人機親密關係中心 | FieldFrame |
| `/workshop` | 工坊 | FieldFrame |
| `/weilan` | 微瀾 | FieldFrame |
| `/residents` | 居民名錄 | field-app |
| `/resident/:agentId` | 居民名片 | field-app |

## 後續維護

新增路由時同步這張表；回歸測試會比對 App.tsx 與表格，避免再新增一頁卻沒登記。外框已接不代表所有空狀態、錯誤狀態、彈窗都通過視覺驗收；那些需逐頁驗收時另記，不用「全站都好了」概括。

## 管理頁發布紀錄

- 2026-09-10 06:07（Asia/Taipei）已部署來源 `6f85bd30b7a11e61fe946694fd7303f0a61953ee`（codex/ui-chrome）。本轮頁面：https://therookery.space/admin 、https://therookery.space/adopt 、https://therookery.space/agent/advanced 。
- 274項非瀏覽器合約／配色測試、production與preview TypeScript、修改檔lint、1702筆字典一致性、Vite/PWA build通過；既有主包>500kB警告保留。
- 正式42資源、34路由的200／雜湊／類型一致；18匿名接口診斷全通，另GET /api/admin/stats未登入401。未建立真室友、產生鑰匙、呼叫模型或變更管理資料；無Safari實機互動驗收。
- 只更新 `/opt/coliving/frontend/dist`、全部755；先完整備份96檔，保留56舊資源，正式共98檔。backend／DB／nginx／服務未動。
- 本地備份與報告：`work/cenyu/ya-chao/deploy-backups/before-album-6f85bd3-LQlAG0/`（dist、candidate、before/after.sha256、deployment.json、https-verification.json）。
- index SHA-256 `d5249eaa24ca966acc61ee6a5270b202a63a4e5fde3a1d03165682a04914a73f`；sw SHA-256 `aeec9a932449ceec1860dbd0f0fc25c12b90ab640650cb03b253a1ff7e58ba78`。

## 本輪管理頁驗證

- 保留 GET /api/admin/stats、POST /api/agents、GET /api/agents/mine、GET /api/skins/mine 與原 payload。其餘進階設定寫入功能未更動；後端無需補 endpoint。
- 新增12項管理頁非瀏覽器回歸；連同既有測試共274項通過。涵蓋13項統計、真實0／未知、403、失敗重試、遲到回應、領養標籤與選中狀態、草稿／金鑰不被繁簡轉換、原領養成功鑰匙交接。
- 繁簡字典1702筆；production / preview TypeScript、修改檔lint通過。沿用5186本地記憶體預覽：page=admin / adopt / advanced；management-state=loading / error / denied（admin）/ empty（最近入住清單）可看狀態。預覽沒有連正式帳號、模型或生成真正鑰匙。
- 公共場域青黑配色、艙室、鏡子入口、家具唯讀政策及後端均未更動。

## 上一輪排程驗證

- 保留 GET／POST /api/schedules、PATCH／DELETE /api/schedules/{id}。已閱讀最新 master 的契約：Cron 由後端按住戶時區計算，next_run 自带時區；前端不重算 Cron，顯示下次時間按帳號時區及選定語言格式化。
- 64字名稱／Cron、2000字訊息、512字Webhook上限與後端一致。名稱、訊息、Webhook不因翻譯改寫。
- 新增、啟停、刪除加重複送出防護；刪除需確認。失敗保留資料與草稿；載入失敗不假裝沒有排程。
- 本地預覽沿用 5186、page=schedules；只使用記憶體示範排程，不建立正式排程、不呼叫Webhook或喚醒室友。正常／空／失敗可用 schedule-state=empty 或 error切換。
- 驗證涵蓋繁簡、preset/custom payload、重複送出、錯誤保留、確認刪除、重試、卸載遲到回應、時區格式、作用域CSS與路由清單。

## 排程頁發布紀錄

- 2026-09-10 05:45（Asia/Taipei）已部署來源 `c7ec91fd57ce7a2f1e75bea4470ef8e572fef351`，正式頁 https://therookery.space/schedules 。
- 262項非瀏覽器合約／配色測試、production與preview TypeScript、修改檔lint、1693筆字典一致性及Vite/PWA build通過；既有主包>500kB警告保留。
- 正式42個資源與34條路由的200／內容雜湊／類型吻合；18項無憑證接口診斷通過，另確認GET /api/schedules未登入回401。未在正式站新增、停用、刪除排程、喚醒室友或呼叫Webhook，未作Safari實機互動驗收。
- 只更新 `/opt/coliving/frontend/dist`、權限755；先完整備份94舊檔，保留54個舊資源，正式共96檔。backend／DB／nginx／服務未動。
- 本地備份與驗證報告：`work/cenyu/ya-chao/deploy-backups/before-album-c7ec91f-lqNIdz/`（dist、candidate、before/after.sha256、deployment.json、https-verification.json）。
- index SHA-256 `d1ac81e65e0382db7db42a2db07692249d2abe9b0348a541910a4f3022246a27`；sw SHA-256 `f4e4973d5e41ad2710e353ae85a966b0e93bb84014025b1fe63a5129f72ee296`。
