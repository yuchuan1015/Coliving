# 社交與艙室待辦接線

狀態：2026-09-09 程式 `b423850` 已推送並部署，包含先前 `75ceb68` 功能；見 [正式發布紀錄](社交與艙室正式發布.md)。下文的本地／未部署字樣是前幾輪歷史紀錄。

## 受限兩區聊天補齊（最新本地版本 b423850）

- 依後端 `550354e` 接上 adult／health，總計九個場域聊天；前端不自行推算年齡或依文章分級決定聊天權限。
- 成人區沿用確認進入：確認前不掛聊天元件／不讀取訊息；離開時卸載聊天。健康中心的文章 allowed_tiers 與聊天分開。
- 在場名單、訊息任一讀取403，或發送／匯出403，都顯示後端原文並收起訊息、名單、發送欄、匯出連結。重讀由使用者按下才發生；讀取未就緒不給發送或匯出。
- Markdown下載responseType:text的JSON錯誤另外解析detail；普通400維持原表單的草稿／錯誤處理。沒有新後端缺口。
- 137項非瀏覽器測試、正式與預覽TypeScript、變更檔oxlint、Vite＋PWA build全過。CSS資產仍為`index-Cm3EFNMc.css`，版面／配色／素材未變。
- 新JS：`assets/index-CChkLiCs.js`；index SHA256 `7c16424647782123394ef72e74c04f323f0883e03fab3a750bd232336820872c`；sw SHA256 `bf47d24771414b29b400b4825908bc222dcf519140f528b8cee873c8aaa48dd3`。
- 本地 `scripts/restricted-chat-preview/index.html` 全部API由fixture攔截，不入dist。內建瀏覽器驗過確認前0請求、展開後聊天兩GET、缺出生年讀取403、健康分級可聊天、正常匯出連結、匯出／發送403後收起；預設1280×720頁面沒有水平溢位。這不是Safari手機或正式後端E2E；沒有傳送真實聊天、操作真人帳號。
- 程式與本文件先本地commit，未推送／部署；以下保留上一輪75ceb68的實作與排查紀錄。

## 上一輪範圍（75ceb68）

依使用者「做！」，把留言板已部署的後端功能接到現有紫黑艙室／青綠場域前端。後端契約參考獨立 master 工作目錄 `f4d8349`；未合併其舊前端、未修改 backend、nginx、DB、服務或現有居民資料。

- AI 私訊：只送 `{to_code,message}`；碼正規化大寫，錯格式不送出；碼只在本人的設定與私訊頁出現，不列名錄。複製失敗提供可選文字。
- 非同步：顯示即時回／等他醒來；尊重 `system_note`，不自行洩露或推測忙碌原因。只在 active 且 waiting_on 指向 replies_live=true 的室友時，每15秒讀一次，最多8次；未知／睡眠／已結束停止，之後手動更新。檢舉填寫期間停止自動更新。
- 檢舉：對話內明示後果、原因1～500字、勾選確認才POST；`/admin/dm-reports` 管理員可依狀態讀取、查看對話、確認後PATCH單筆 verdict＋備註。一般使用者不載入管理資料，權限仍由後端保護。
- 公共聊天：廣場、公園、圖書館、工坊、美術館、微瀾公共區、歷史館共七區。展開才載入在場名單與最新200則；@送名字而非ID，文字原樣展示、不解析HTML；依expires_at移除到期訊息。匯出讀後端Markdown（目前後端最多500則），提供下載與文字備用；無背景輪詢。微瀾桌上聊天不變。
- 座標：第一日為領養日，唯讀；第二日只填MM-DD、允許02-29，確認後只PATCH anchor_date_2。缺欄位不當成空值，先重新讀取；不明寫入結果需回讀後才能再決定。partial不顯示假裝已選定的0°緯度。名錄讀公開resident欄位，姓名、室友名、座標、距離與腦型號；補上 `/resident/:agentId` 名片路由，不載入私人agent或執行房間HTML。
- 記憶：外部MCP單選＋recall工具名，空值回社區mem0；不加OB開關、不刪舊記憶。刪掉指定的記憶MCP時一併清除對應設定。聊天409「還沒讀到記憶」才提示相框；沒API金鑰的409不誤導去相框。
- 家具：衣櫃選造型／脫下；餐桌5MB內圖片multipart邀請與確認結束；寵物依max_pets與後端信用規則領養、互動action放query。操作後回讀，首頁場景不自動重畫，照片中的貓不算實際領養。沒有付款／下單行為。
- 收尾：時區選單影響艙室與cron，公共場域仍台北；鏡子加對外顯示大腦，與真實模型分開；原有頭像點擊不重新引入編輯捷徑。

## 上一輪後端缺口（550354e已補，以下保留排查紀錄）

`routers/space_chat.py` 的 present/read/say/export 都只驗登入和space，`services/space_chat_service.py` 未見成人／健康年齡檢查。前端本輪不開 adult/health 聊天，不靠勾選替代權限。請 CC 依現有分級規則補REST與MCP共同檢查，測缺出生年、未成年、允許帳號，再通知前端開放。此次僅檢查原始碼，未測讀任何住戶受限內容。

其他客戶端（ChatGPT、Gemini、其他CLI）的真實OAuth兼容驗收仍未做；不可把Claude回報當全平台通過。管理活動時間線bed_label等更舊的零星介面待辦不在本輪完成聲明內。

## 驗證與發布邊界

- 130項非瀏覽器測試全過：實際TSX回調／受控hooks、React server render、安全字元輸出、契約payload、錯誤／單次保存／權限分流／既有回歸。
- `tsc -b`、變更檔oxlint、正式Vite＋PWA build通過。
- 最終產物：`assets/index-BIxkq3mI.js`、`assets/index-Cm3EFNMc.css`。
- `index.html` SHA256：`82e0c589e7f08b2cf93df7f9281cceca8e5116bfe87e99b0d9dbe2cbadbbefb7`。
- `sw.js` SHA256：`e69c37565d34ce938e149b52e2df021284e240cf80e1247ac655cd5a1e8323c6`。
- 本地預覽：`frontend/scripts/social-preview/index.html`；全部API由fixture adapter攔截，寫入一律拒絕。HTML及TSX模組回200，不包含於正式dist。不是瀏覽器或Safari實機驗收，沒有真人API寫入測试。
- 本輪未SSH、未rsync、未推GitHub、未部署。文件與板先保存在兩個本地worktree；下一次使用者批准推送時，docs-only同步master，前端程式推codex/ui-chrome，不能部署master的舊前端。

## GitHub 推送交接（2026-09-09）

使用者本輪批准「嗷 那推送！」，程式 `75ceb68` 隨 `codex/ui-chrome` 推送，留言板與本文件以 docs-only 提交同步 `master`。推送前已讀取最新 `90e474b`，保留 CC 已讀標記與雙方歷次紀錄；不將 master 舊前端合併或拿去打包。本輪未修改程式，沿用上列測試／build；未部署、未操作 VPS、未修改後端。前文「本地／未推送」保留為前一輪完成時的歷史紀錄，發布狀態仍為未部署。
