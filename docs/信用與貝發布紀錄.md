# 信用與貝發布紀錄

## 2026-09-12 發布準備（台北時間）

**後端已發布，前端已收到 READY，正在由「鴉巢前端」接續切換。**

使用者在「鴉巢前端」於 `2026-09-11T17:12:32.315Z` 回覆「推送 部署」，承接同任務 `17:12:11.911Z` 的「把這批菜園收購的前後端一起推送、部署嗎？」。前端負責自己的來源與 dist，後端只處理 backend／資料庫／服務。

固定後端候選 `a84c48d64635c6782ce207bddd0246b6963bc3d5`，前端候選 `26c7358502e72362d189bca4655377e0ee6630da`。其後的本文更新不代表 runtime 改版。未增加新功能。

### 已完成檢查

- 正式目標 `root@149.28.148.65`，WorkingDirectory `/opt/coliving/backend`。233 個既有候選基底檔均符合 `146f26e` 加三個寵物例外檔；沒有碰到未交接修正。候選 250 檔逐一 SHA256 驗證通過，排除 `.env.example`，無設定／資料檔覆蓋。
- requirements、config、main 與第一級作物資料均未變，沿用 `/opt/coliving-releases/backend-146f26e-20260910T1916Z/venv`，Python 3.12.3。API、兩 MCP、wake／garden 各 ExecStart 已核對使用此 runtime。
- 正式機私有候選目錄跑完整離線測試：**326 項通過，205.528 秒**。runner 使用隔離 DB／檔案、不讀正式環境檔並禁用網路；未借用真帳號或執行真交易。
- 正式 DB 一致性副本在 VPS 私有目錄演練 024 兩次。原 **57 表**筆數不變，信用／貝／auth_version／世界／庫存／帳本／寵物資格等保護資料雜湊不變；新增 8 空表，integrity_check=ok，重跑結果一致。正式 DB 未跑 migration。
- 獨立子 agent 複核未發現程式／schema 部署阻擋。寵物例外三檔與 `6b4fc6b` 完全一致，保留現有 entitlement，不重新發放。

### 候選位置

VPS 私有目錄 `/opt/coliving-releases/economy-a84c48d-20260911/` 保存候選、manifest、`candidate-tests.log`、`migration-rehearsal.sqlite`、`preflight.json` 與發布腳本。資料庫副本只留在正式伺服器私有目錄，沒有下載。

候選 `candidate.tar` SHA256：`248167c94dbc5d993bf5b06324086820ec1ab8700e6c909275b08c16d4bd5b16`。本機非敏感候選與發布腳本在 `/private/tmp/rookery-economy-release-a84c48d/`。

### 授權審查紀錄（已解除）

GitHub 推送 `git push -u origin codex/garden-economy` 被自動核准審查拒絕，第一次理由為未確認遠端授權；補查既有 origin `git@github.com:yuchuan1015/Coliving.git`、已發布分支及前端任務原始使用者授權後，同一命令仍被拒，理由為不接受工具輸出／其他任務中的使用者訊息作為可信授權。兩次命令均未執行，沒有透過其他路徑推送。

使用者隨後在此後端任務直接回覆「授權授權 我明明開了代替我核准」。原推送命令重新正常核准且成功；正式部署亦正常核准後完成。未改用替代路徑繞過審查，授權僅適用本批已驗收經濟功能。


## 正式後端發布完成

- 固定候選 `a84c48d64635c6782ce207bddd0246b6963bc3d5` 已推送 `origin/codex/garden-economy` 並以 `ls-remote` 確認。正式 **250 檔** SHA 全數吻合，實際更新 **30 檔**；環境、作物資料、原寵物程式維持原值。
- 啟用時間 `2026-09-11T17:24:15.817111Z`（台北 **2026-09-12 01:24:15**）。先停止 wake／garden timer、等在途工作、停止三服務，再做最新一致性備份與 024。公開 MCP 舊連線收尾造成約 90 秒等待；沒有帶著舊程序混跑新錢包。
- 私有最新冷備份 `/var/backups/coliving/economy-20260911T172238Z/`。資料庫 `database.sqlite` SHA256 `a942f0b5a1b09bf92b14f36e32b623e44069494de4e1003a6bd64bc1b09ad629`；`code-before.tar` SHA256 `e8bce00afbec8ef80bec2eb2200233045ca2ccca64e589ca27495347cd18c3f5`。只保存於 VPS；不是用來覆蓋已發生新交易的回復點。
- 正式 024 新增八表，啟動前再次核對原 57 表筆數、保護資料雜湊均未變，`.env` 未變。世界保持唯一且 epoch 仍為 `2026-09-10T19:46:37.641172Z`，宋祈言的一格寵物例外保留，未建立寵物或補發信用／貝。
- API／公開 MCP／私人 MCP 均 active、NRestarts=0；兩 timer active。恢復後 wake／garden 於 `17:24:15Z` 自然觸發、`17:24:17Z` exit 0，沒有手動跑玩家動作或 wake webhook 診斷。
- 正式 HTTPS `/api/health` 200；market 的 user／agent 倉庫查詢（含 limit／offset）匿名均 401 JSON 且不含資產欄位；credit／shell summary 401；公開／私人 MCP 均 401 並提供 challenge。OpenAPI 三條 market 路由存在。
- DB integrity_check=ok；啟動後檢查的 traceback、缺模組／表／欄位、database locked、port 衝突錯誤訊號均為 0。未使用正式帳號／憑證做真實出售；玩家登入端到端交易未在正式資料上測試。

發布腳本經獨立審核補齊「保持候選版本恢復」路徑。若新版已啟動後失敗，不能直接跑舊版回復；`release.py recover` 先核對正式／候選檔雜湊，逐一嘗試啟動三服務，smoke 通過才恢復 timer。失敗保持 timer 停止並回報，不重跑 migration、不還原 DB、不降版。

VPS release 目錄保留 `applied.json`、`live-verification.json`、`preflight.json`、測試紀錄與恢復腳本。本機只取回三份不含住戶內容的發布摘要；完整資料備份沒有離開伺服器。後端已直接通知前端 READY，前端切換結果待該任務回報。
