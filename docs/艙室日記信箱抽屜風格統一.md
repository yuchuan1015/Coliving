# 艙室日記、信箱、抽屜 · 統一風格

2026-09-09。**已推送部署 232251b**。正式站已更新為三頁統一紫黑風格，舊版55abecc已完整備份。下方保留開發階段紀錄，發布結果見文末。

## 範圍

- 日記本 /home/diary、抽屜 /home/drawer、星際信箱 /mailbox；沿用相簿已確認的紫黑星空、玻璃卡片、細邊框、圓角按鈕和表單樣式。
- 三頁共用 CabinUtilityShell，直接匯入既有 photo-album.css，再用限定範圍的 cabin-utility.css 補列表、表單、信件閱讀的排版。沒有更改相簿樣式檔、登入、主艙室、三區圖片、家具座標、公開郵驛或其他場域。
- 統一標題與「返回艙室」，欄位有可讀標籤；主要文字／輸入為16px，輸入高48px，長字串換行，小螢幕標頭可換行、返回按鈕四字不拆行。
- 日記：搜尋、寫日記、保存／取消、展開全文、刪除保留。
- 抽屜：物品名稱、內容、選填分類、存入／取消、展開、丟掉保留。不加入新公開／私密切換，migration017 的兩入口語意不變。
- 信箱：收件／寄件／寫信、未讀標示、讀信、匿名寄送、送達時間與刪除保留。收件列由可點 div 改為原生 button，讀信內保留回信箱，另外有統一返回艙室；既有錯誤訊息在閱讀狀態也可見。
- **沒有把 /mailbox 導向 /mail**，沒有增加定時／實體寄送功能或改變分頁流程；本輪只先統一視覺。出艙後郵驛保持原狀。

## 驗證與邊界

- 逐頁比對：去除不再使用的 navigate 宣告後，原本所有 state、effect、資料讀寫 handler 段落與修改前完全一致。API 模組、路由、授權與 backend 無 diff。
- 新增10項非瀏覽器回歸：共用外殼／返回、載入與空白狀態、日記搜尋／展開／保存／刪除／取消、抽屜儲存／選填分類／取消／刪除、信箱惰性讀信／分頁／未讀／錯誤／刪除／收件人／匿名寄信／字數限制與樣式隔離。
- 合計180合約＋5配色＝**185項通過**。production與preview TypeScript通過，Vite build通過；主JS504.55kB，原有500kB分包提示仍在。
- 修改檔lint保留2個既有警告：DiaryPage的set-state-in-effect、MailboxPage的loadData宣告順序提示。取數流程本輪未重寫，也未聲稱所有舊bug已修復。
- 依既有介面調整流程重用元件，未另建Sites／換框架或套新模板。這輪未做瀏覽器點擊／截图／Safari真機測試；以型別、合約、原處理流程比對和本地預覽HTTP200驗證，不宣稱視覺端到端驗收。
- 本地預覽補三頁的記憶體假資料，文字明示範例，不連真實帳號。預覽仍在scripts/，不進正式dist；可從導覽或艙室家具開啟。

## 預覽與交接

- http://127.0.0.1:5186/scripts/album-preview/index.html?page=diary
- 同頁導覽可換日記本／抽屜／星際信箱，也可用 page=drawer 或 page=mailbox 直接查看。
- 開發階段先本地保存並在工地留言板記錄，當時未SSH、未動VPS；使用者隨後批准推送部署，結果見下方。沒有需要CC修改的接口。

## 正式發布紀錄

- 使用者於2026-09-09批准「推送 部署」。前端來源 `codex/ui-chrome` 的 `232251b552d88cac7790f740af6af370e7eeb556`，已先推送來源再將驗證過的build產物上線；發布前重跑185項測試、production TypeScript及Vite build通過。
- 2026-09-09T04:06:17.593Z切換正式入口。只更新 `/opt/coliving/frontend/dist`，先完整備份86檔、驗證暫存與舊站未被並行改動，再替換index及service worker；保留46個舊資源、沒有刪檔，線上88檔雜湊及所有檔案／目錄755權限通過。未動backend、DB、nginx或重啟服務。
- 本地可回復備份：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/before-album-232251b-n7IhLK`。內有舊dist、candidate、before/staged/after SHA256清單及deployment.json。
- 新資源：`assets/index-C2QaZD9_.css`、`assets/index-CCK1hR1_.js`；index SHA256 `3e6198f7303076378e6d1435a245487345ca247764af975176830cd1c3041b06`；sw SHA256 `e765ed31d6eb1b48a620178c786315ed141bc0135e3ae9e3840d368f0469de7a`。
- 公開HTTPS驗證：42靜態檔＋32個SPA入口（含/home/diary、/home/drawer、/mailbox）全部200且雜湊／MIME正確，18項公開／未登入診斷符合預期。報告：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/utility-232251b-verification.json`。這是靜態入口與匿名診斷，沒有讀寫真實帳號資料，不等於登入後瀏覽器／Safari真機驗收。
- 部署紀錄與留言同步docs-only工作區並推master；前端完整來源保留codex/ui-chrome，勿從master舊前端build覆蓋。本輪沒有新接口或需CC配合的後端改動。
