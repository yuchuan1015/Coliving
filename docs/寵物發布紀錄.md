# 寵物功能正式發布（2026-09-12）

寵物圖庫、普通領養、照顧修正與許願流程已完成前後端聯合發布。喻墨在本後端任務直接回覆「授權授權！」後執行；後端只處理 backend、026 遷移與三個服務，前端自行發布 dist，未修改 nginx。

## 固定版本與範圍

- 後端：`0824dcd6c002052ad28817638e35152d3a1124a2`，已推送 `origin/codex/pet-integration` 並以遠端 SHA 確認。後續文件提交不代表正式 runtime 改版。
- 正式前端：`39963d7586792ecda1479cf15df92588d597be7b`，前端任務於 `2026-09-11T22:21:31.842Z` 完成發布並交付 STATIC READY。
- 正式後端原基底：`a84c48d64635c6782ce207bddd0246b6963bc3d5`；250 個原檔 SHA 全數吻合，沒有新路徑碰撞。候選 266 檔全數核對，實際更新 24 檔；requirements、設定程式與既有經濟功能保留。
- 目標 `root@149.28.148.65`，後端 `/opt/coliving/backend`；沿用 `/opt/coliving-releases/backend-146f26e-20260910T1916Z/venv`，未更換 Python 或依賴。
- 圖庫版本 `rookery-pets-v1-20260912`，28 張 `/assets/pets/v1/{id}.png`。切換後端前逐張正式 HTTPS 200、`image/png`、內容 SHA 與前端權威清單／驗收紀錄一致。

## 發布前驗證

正式機私有候選目錄跑完整離線測試：**389/389 通過，240.763 秒**。使用合成帳號、暫存資料庫，禁止對外連線、不讀正式 `.env`。獨立子 agent 複核 runtime、遷移與發布腳本，沒有未解 P1/P2。

026 在 VPS 私有一致性副本演練兩次，原 65 張表的所有舊欄位、資料筆數及逐列雜湊不變，原 schema 保留，只新增 `pet_wishes`／`pet_wish_receipts` 兩張空表及 `pets.asset_key` 可空欄位；第二次與第一次結果完全一致，integrity_check=ok。

首次副本演練被發布腳本的精確 SQL 比對擋下：SQLite 將新欄位放在 PRIMARY KEY／FOREIGN KEY 表級約束之前，原預期字串把它放在最後括號前。改以同一 SQLite engine 從原 schema 產生預期 SQL，再精確比對；獨立複核包含「僅竄改外鍵目標仍須被拒絕」。修正只在發布工具，不改候選 runtime；失敗副本保留，正式資料未受影響。

通過的副本：`/opt/coliving-releases/pets-0824dcd-20260912/migration-rehearsal-1789166241255760949.sqlite`。副本與備份均未下載。

## 正式切換與驗收

啟用時間：`2026-09-11T22:39:44.589326Z`，台北 **2026-09-12 06:39:44**。

先停止 wake／garden timer、等在途工作結束、停止三個後端服務。公開 MCP 的既有連線收尾等待約 90 秒；沒有強行與舊程序混跑。停止寫入後做新的一致性備份、執行 026，再次確認全部舊資料保留，才替換 24 個後端檔案並重啟。

- 正式 266 檔 SHA 全吻合候選，`.env` 雜湊未變。
- 三服務 active、NRestarts=0；兩 timer active。兩排程於 `22:41:00Z` 自然執行且 exit 0，沒有手動觸發玩家動作或排程。
- 公網 `/api/health` 200，六個寵物 API 路徑／方法出現在 OpenAPI；圖庫匿名查詢 401，公私 MCP 401 且有授權 challenge。
- 可信 registry 28 筆、版本正確，026 schema 存在，正式 DB integrity_check=ok。
- 啟用後 traceback、缺表／欄位、database locked、缺模組、port 衝突訊號均為 0。
- 沒有使用真人帳號測試領養、許願或照顧，也沒有用可能 lazy tick 的 `GET /api/pets` 驗證；沒有建立正式寵物、回填舊圖、補發信用或更動宋祈言的資格。

## 備份與恢復

冷備份：`/var/backups/coliving/pets-20260911T223807Z/`（僅 VPS 私有保存）。

- `database.sqlite` SHA256：`a0d6212b0da1eb73f81b77f0cd572bbf342ca30780fd3f1022b127b775e73a69`
- `code-before.tar` SHA256：`3bd34980061d6768e1a34a89a16aed5875724ffd1e38682d031ffc344fc0dc16`

新版開始接受請求後，**不能直接還原舊 DB 或回退舊領養程式**：舊版不計算許願保留名額，會造成超額領養。發布工具在啟動第一個新服務前就標記 exposed；此後只允許同候選版本向前恢復。`release.py recover` 僅用於已記錄的啟用後失敗，核對檔案／環境／圖片後啟動相同版本，smoke 成功才恢復排程，不重跑 migration、不還原 DB。

發布私有目錄：`/opt/coliving-releases/pets-0824dcd-20260912/`，保留 manifest、候選、389 項測試紀錄、遷移副本、preflight／applied／live-verification／post-start-diagnostics JSON、release.py 與 verify_live.py。本機非敏感工具及摘要在 `/private/tmp/rookery-pet-release-0824dcd/`。

候選 tar SHA256：`3190bf0ff642ed93349ccef957a4b01c4cef80b30b1127c3d1478414592c1249`。manifest SHA256：`ce0a818f9f010434e28436d3f11144e72daa91ce2256ae229d0bdeeec55691cf`。

前端於 `2026-09-11T22:41:48.759Z` 完成發布後 36 項檢查，全數通過：首頁、管理許願頁、SW、catalog、28 張 PNG 與 `39963d7` 的 SHA 相符；health 200；圖庫、居民許願與管理許願匿名均為僅錯誤欄位的 401 JSON。未呼叫 `/api/pets` 或執行正式寫入。
