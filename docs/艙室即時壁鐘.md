# 艙室壁鐘 · 即時指針

2026-09-09。**已推送部署29dbda7**。正式站即時壁鐘已上線，舊版232251b已備份。下方保留開發紀錄，發布結果見文末。

## 實作

- 保留生活區原底圖與壁鐘金屬外框，只在原錶盤內覆蓋可即時更新的功能型SVG刻度／指針；不產生新圖片、不改三卡尺寸、底圖曝光、其他家具或錨點。
- 錶盤依原圖1053×1494的52×54內徑換算，與既有object-fit:cover倍率同步；中心仍是clock錨點(.782,.335)。外層44px原生按鈕不縮，移除會遮住錶面的紫色圓點。點鐘仍展開卡片，進入仍開當地／社區時間彈窗。
- 時／分／秒針依每次實際Date及住戶IANA時區計算，不用固定動畫累積；時區優先user.timezone，其次家具clock.timezone，再沿用瀏覽器時區。社區時間仍Asia/Taipei。
- useCabinTime共用給頂卡、錶盤與原時間彈窗，每秒對齊更新；頁面隱藏時取消計時，visibilitychange／focus／pageshow恢復立即校時且只保留一個計時器，卸載清理。沒有時鐘API輪詢。時間準確度沿用裝置系統時鐘，並未新增伺服器授時。
- 秒針直接更新角度，跨59→00不倒轉一圈；prefers-reduced-motion下隱藏秒針，時分針仍反映時間。SVG不搶焦點、不攔截按鈕；不逐秒朗讀。

## 驗證

- 新增10項回歸，總計195項全通過：分針／時針進位、午夜／正午、台北／加德滿都／紐約夏令時間、錶盤cover比例、按鈕及原卡片／彈窗、慢回呼校時、休眠恢復、卸載清理、減少動態效果。
- production及本地預覽TypeScript通過，修改TS/TSX檔oxlint退出0，Vite build通過。原500kB分包提示仍在（新主JS506.45kB），不是build失敗。
- 沿用Sites既有介面調整流程，只擴充現有React艙室與隔離假資料預覽，不另建網站、不改依賴。預覽入口與小鐘模組均HTTP200；未做瀏覽器點擊／截圖或Safari真機驗收。預覽開啟請求為queued，不宣稱已在使用者螢幕顯示。
- 預覽：http://127.0.0.1:5186/scripts/album-preview/index.html?page=clock（開生活區）；所有資料為本地假資料，scripts不進正式dist。
- 開發階段未動backend／API契約／DB／VPS；使用者隨後批准前端部署，結果如下。沒有需要CC配合的後端工作。

## 正式發布

- 使用者於2026-09-09批准推送部署。來源 `29dbda70e1ee18c9a8c5d52fa9cdd9eb5af309ea` 已推送codex/ui-chrome。發布前重跑195項測試與production TypeScript，沿用前輪相同來源已驗證的build，未另改UI。
- 2026-09-09T15:09:36.974Z啟用。只更新 `/opt/coliving/frontend/dist`，完整備份88檔、上傳42檔、保留48個旧資源，線上90檔全雜湊比對通過；所有檔案／目錄權限755，無symlink。index與service worker先暫存驗證、再各自原子替換，沒有刪除舊資源或重啟服務。
- 本地可回復備份：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/before-album-29dbda7-w9tUWd`，含舊dist、candidate、before/staged/after SHA256清單與deployment.json。
- 新資源：`assets/index-CeQy7wkp.css`、`assets/index-Ob_ucEhW.js`；index SHA256 `7e8c6cf87d5e85902a204af4fd9213a66787207721e6de0b046373ecca84d9fc`；sw SHA256 `13ccac4155dcc5548a2a95cda6741fad3541bd46bf53dfa21a91d3a7a44ec3d5`。
- HTTPS公開驗證42靜態檔＋32個SPA入口全部200、雜湊／MIME正確；18項公開／未登入診斷符合預期。報告：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/clock-29dbda7-verification.json`。沒有讀寫居民資料，也不是Safari真機／登入後端到端驗收。
- 發布紀錄與留言同步master（已先整合CC的e7ca7de文件更新）；前端完整來源仍在codex/ui-chrome。後端、DB、nginx和服務均未動。
