# 鴉巢前端交接整合 · 2026-09-10

本輪只整合、驗證與本地提交，**沒有 git push、SSH、部署、migration 或正式資料變更**。工程交接直接回覆後端任務 `01a08bb0-1a2d-7540-bfd6-60ba7d00e4d8`（local）；工地留言板保留歷史，不再新增交辦。

## 來源與整合

- 前端工作區：`work/cenyu/ya-chao/reconcile-ui`，分支 `codex/ui-chrome`。
- 整合前是乾淨的 `cc23073`；交接 `codex/frontend-handoff` 正好以它為基底。
- 以 `git merge --ff-only codex/frontend-handoff` 接入 `fa8391c`、`163fbd9`、`03c2cf57ed3955a1325ddafd477573567103389d`，無衝突、無改寫歷史，未切動共用 checkout 或交接工作區。
- 已接收：帳號顯示名稱、改密碼後重新登入、親密中心伺服器分類／分級篩選及載入更多、權限失效時撤下全文、舊登入／refresh／個人資料回應競態修正。沒有重做畫面或配色。

## 整合時補修

新增 `AuthContext.updateDisplayName` 後，cabin-theme、album、social 三個既有隔離預覽的 Provider 少了該方法。production TS 不包含這些入口，額外預覽 TS 檢查才重現。

三處均接既有 `readOnly`／`denied` 函式，不讓預覽改真實資料；另加一項回歸檢查。

子 agent 唯讀複核另重現全文端點 401／403 的清除缺口：列表先成功、開全文才拒絕時，列表與投稿仍在。主 agent 補修 `ArticlesField`：同次 render 撤下列表／全文／投稿，保留失效狀態至明確重新讀取，等待新的列表授權成功才恢復；離開後重入也重讀。新增一項同時涵蓋 401／403 的回歸，並把全文 403／重新驗權加進三尺寸 Chrome 腳本。後端任務確認沿用現有接口，不需改後端。

## 本輪驗證

- 交接原有 341 項合約／配色測試通過，兩項整合補修後 **343 項**全數通過。
- **1816 筆**繁簡介面字典檢查通過。
- production TypeScript／Vite／PWA build 通過。
- cabin-theme 專用 TS 及 album／social 入口完整 TS 檢查通過。
- 修改範圍 oxlint 無錯誤；保留既有 AuthContext 與 social preview 的 Fast Refresh 提醒、主包超過 500 kB 的提醒。
- 重跑 `frontend/scripts/handoff-smoke.mjs`：隔離 Chrome 的 **320／390／1280px**，改名去頭尾空白、改密碼錯誤與成功重新登入、20→40→45 篇文章、後端分級篩選、下一頁 403 撤下文章、全文 403 立即關閉全文／撤下列表和投稿、明確重新讀取後恢復均通過，無橫向溢出。
- 瀏覽器全部 API 都為攔截測試資料，封鎖外網，未用真帳號或供應商。最終截圖在 `/tmp/rookery-frontend-integration-final/`，主 agent 先已查看 390px 帳號及文章畫面，補修未改 CSS；不是 Safari 真機驗收。console 的測試 400／403 為預期 fixture 回應，不是正式站錯誤。
- `git diff --check` 通過；本輪整合不包含後端檔案或 CSS 變更。

本地 build 已包含交接 03c2cf5 及上述全文失效補修，最終本地提交見本文件所屬 commit；下次發布前仍須按最終 HEAD 重新核對：

| 檔案 | SHA-256 |
| --- | --- |
| `dist/index.html` | `f8b928d223b4ee4d24d816617d14c5828a219142c74a7ae7a140835a4875aba6` |
| `dist/sw.js` | `02776c7e8e75c7868a63ef37fd691afc92a422f41fd9d8f803de665afc575900` |
| `dist/assets/index-BVW-pa9I.js` | `75df03b35195549af3c6b8592cf35a9cd1afe0864d0ce714e7009566bba83b1b` |
| `dist/assets/index-GYrUqda_.css` | `9ee4c417623bc1abe7f4caac609c622b75c09de617ed801f53989710c0ab8807` |

## 後端與發布依賴

已唯讀核對本機後端 `08f5308` 的 users／adult router 與 schemas，當前功能不需再新增 endpoint：

1. `PATCH /api/users/me {display_name}`。
2. `POST /api/users/me/password {old_password,new_password}`，成功後舊網頁憑證失效，MCP／OAuth 保留。
3. `GET /api/adult` 接 category／age_tier／limit／offset，先授權篩選再分頁，回 has_more／next_offset；無篩選時省略參數。

**不得把本地完成寫成已上線，也不能單獨先部署前端。** 下次取得發布授權後，先依 [後端修復部署準備](後端修復部署準備.md) 備份、migration 022、更新 API／兩個 MCP／排程並驗證，再發布此整合分支。不能用 main 的舊前端打包。
