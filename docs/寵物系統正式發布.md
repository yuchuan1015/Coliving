# 寵物系統正式發布 · 2026-09-12

**前後端均已推送及上線。** 喻墨驗收寵物介面與透明圖資後，明確授權「推送 部署！」。本批包含領養／照顧彈窗、不可更改且占名額的許願流程、管理員準備及確認到家，以及 28 種透明寵物圖。

## 前端已上線

- 來源：`codex/ui-chrome` / `39963d7586792ecda1479cf15df92588d597be7b`，已正常推送 `origin/codex/ui-chrome`。功能提交為 `74bd79f`、圖資提交為 `73f79bc`；未包含工作區原有的 AGENTS 修改、白底原圖與未追蹤規格。
- 從乾淨發布副本建置。540 項非瀏覽器測試、TypeScript、2393 條繁簡文案、28 張 RGBA 圖資閘門與 production build 均通過；既有 JS bundle 體積提醒仍在。
- 正式前端於 `2026-09-11T22:21:31.842Z`（台北 09-12 06:21）啟用，只更新 `/opt/coliving/frontend/dist`。先完整備份原 120 檔，逐檔 SHA 與 rsync checksum 乾跑核對；發布 73 檔、保留 78 個舊資源，正式共 151 檔，SHA 與 755 權限全數通過。
- 資產先上傳，驗證暫存入口及併發守衛後才各別原子切換 index / service worker。沒有使用 rsync `--delete`，沒有修改 nginx 或覆蓋艙室原素材。
- HTTPS 檢查：73 個 build 檔內容與類型、38 條 SPA 路由、23 個匿名診斷均通過。其中 28 張 `/assets/pets/v1/*.png` 全部為 200 `image/png` 且 SHA 與本地一致。
- 另不加驗證查詢參數檢查 `/`、`/admin/pet-wishes`、`/sw.js`、`/assets/pets/catalog.json`，皆為 200 且內容 SHA 符合本版。SPA 管理頁回 200 不代表匿名使用者能取得管理資料。
- 圖库清單版本：`rookery-pets-v1-20260912`；SHA256：`1ceca794f8b6a9ae48907e82dc8dc084cf3acb5c20ea404d8cdefe7910a4e8a4`。28 張網站圖共 4,136,329 bytes。PWA precache 仍為 20 項，沒有預下載整批圖片。
- index SHA：`0ce18db2d4475ed8f5293361ed01e98986eda83b2389a414a13d13a427b479d6`；sw SHA：`50807c3ce19028b52d19d99be9dc77ff43345acf2bdcc00c0120351f897b68d7`。

## 後端發布順序

後端來源為 `codex/pet-integration` / `0824dcd6c002052ad28817638e35152d3a1124a2`，由「鴉巢後端」任務獨立核對正式基底、備份、migration 026 與服務切換。前端已在確認全部 28 個正式圖址可讀後，傳送 STATIC READY，才放行後端啟用 registry。

後端發布準備回報：來源已推送並以遠端查詢確認；正式 250 個基底檔 SHA 符合 `a84c48d`，候選 266 檔 SHA 通過。Linux 隔離離線測試 389／389 通過（240.763 秒）；026 在新資料副本跑兩次，原 65 表所有既有欄位資料、筆數、雜湊與 schema 保留。獨立 runtime／發布腳本複核無 P1／P2。初次演練的新增欄位 SQL 位置比對修正為使用同 SQLite engine 產生精確預期後重驗，舊演練副本保留，未以放寬 schema 檢查跳過差異。

後端正式完成回報：

- 於 `2026-09-11T22:39:44.589326Z`（台北 09-12 06:39）啟用。正式 266 個候選檔 SHA 全部吻合，只更新 24 個 backend 檔，不改前端或 nginx。切換前再次驗證全部 28 個 HTTPS 圖址、類型與 SHA。
- 停止排程觸發、等在途工作、停止三個服務後完成最新冷備份，再執行 026。原 65 表所有既有欄位資料、筆數、雜湊、schema 保留，只新增兩個空表與 nullable `pets.asset_key`；`.env`、原信用資格及既有寵物不變。
- API／公開 MCP／私人 MCP 三服務及兩 timer 均 active，NRestarts=0。公網 health 200、六條寵物相關 OpenAPI 路由存在、圖庫匿名 401、公私 MCP 401 且有 challenge，DB integrity_check=ok。可信 registry 已是 28 圖的 `rookery-pets-v1-20260912`。
- 私有冷備份留在 VPS `/var/backups/coliving/pets-20260911T223807Z/`，資料庫 SHA `a0d6212b0da1eb73f81b77f0cd572bbf342ca30780fd3f1022b127b775e73a69`，程式備份 SHA `3bd34980061d6768e1a34a89a16aed5875724ffd1e38682d031ffc344fc0dc16`。不下載正式資料庫。
- 私有候選及證據位於 `/opt/coliving-releases/pets-0824dcd-20260912/`：發布及恢復腳本、applied／live-verification／preflight、389 項測試紀錄。啟用後不可退回會忽略 reservation 的舊領養程式，也不可還原舊 DB；按已複核流程使用同候選修復並恢復服務。

## 證據、回退與驗證界線

- 後端 READY 後，前端任務於 `2026-09-11T22:41:48.759Z` 再做 36 項正式 HTTPS 核對，全部通過：首頁／管理入口／sw／清單／28 張圖無查詢參數時內容 SHA 一致，health 200、圖庫／居民許願／管理許願三 API 匿名均 401 JSON 且沒有私人資料欄位。結果保存為 `pet-live-verification.json`。
- 前端完整舊版與精確新 build、前後 SHA、`deployment.json`、`https-verification.json` 保存於本機 `deploy-backups/before-album-39963d7-EeGR73/`，不推送備份。
- 如需回退，先確認沒有較新的發布，再依備份恢復前端入口與必要資產；不可覆蓋別人的後續更新，也不能因前端回退就還原正式資料庫。
- 未借用真實帳號試領養、許願、照顧或管理員確認到家。未把會觸發 lazy 寫入的正式 `GET /api/pets` 當作只讀診斷。HTTP／檔案與隔離資料測試不等於真人端到端或實體 Safari 驗收。
