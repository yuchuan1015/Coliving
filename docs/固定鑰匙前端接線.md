# 固定鑰匙前端接線

2026-09-09 · 本地完成，尚未推送／部署。

## 範圍

使用者要求重拉留言板、急件先做；本輪拉到 master 9bc700f，優先實作最上方「固定鑰匙」留言。程式在 codex/ui-chrome，只讀 master 的 backend 契約，沒有合入 master 舊前端。

## 已完成

- /agent/advanced 的 MCP 區塊改成鑰匙清單：GET /api/agents/mine/mcp-tokens，每把顯示 label、建立時間、最後使用、作廢狀態。
- 兩顆複製鈕分別照回應的 connect_url／claude_code_cmd 原值；不自行拼 token、不更改指令。不是只顯示一次。
- POST /api/agents/mine/mcp-token {label}：名稱去首尾空白、非空最多32字；成功立即加入清單，防連點，不自動重試產生。結果不明提示先更新清單確認。
- DELETE /api/agents/mine/mcp-tokens/{token_id}：兩段確認；成功立即丟棄該把 mcp_token／connect_url／claude_code_cmd，其他鑰匙不變；失敗顯示 detail。
- 領養 POST /api/agents 的 API key 選填，空白省略 api_key；成功顯示 first_key 連接器網址和複製鈕，不直接跳聊天，提供返回艙室／查看鑰匙清單。
- first_key 缺少時引導去鑰匙清單，不叫住戶重新領養。
- 剪貼簿拒絕或缺少 API 時提供唯讀手動複製；只有寫入剪貼簿成功才顯示已複製。
- secret 僅留元件記憶體，不進 storage／log／路由 state／網址列；沒有可點擊的 secret 連結、自動開啟或嵌入。只有主動點複製才進系統剪貼簿；頁面提醒不要公開或截圖分享。

## 不變

登入、艙室三卡／配色／照片／家具錨點／FAB、鏡子唯一資料更新入口、外部 MCP 和房間皮膚、認證與服務配置、backend 都沒改。新 CSS 只影響鑰匙及領養成功區塊，沿用現有冷紫黑 tokens。

## 驗證

- TypeScript 專案編譯、Vite production build 通過。
- 72 項非瀏覽器契約／元件 callback／配色回歸測試通過（原56＋新增16）。
- 覆蓋清單空／錯誤／更新／取消讀取，具名產生與防連點，作廢確認與錯誤，兩種複製與 fallback，作廢或缺欄位，領養選填／first_key／失敗保留輸入。
- 全用假資料、假剪貼簿；未取住戶真鑰匙、未對真實 API 產生／作廢／領養。未做瀏覽器或 Safari 實機驗收。
- git diff --check 通過；登入／艙室／鏡子相關檔案和 backend 無 diff。

## 待辦與發布邊界

- 此急件待批准推送／部署；部署仍只能動 /opt/coliving/frontend/dist，沿用完整備份、先上資產、保留舊資產和手機預覽、755 的既有流程。
- 板上其餘新增：私訊 to_code（舊 to_agent_name 不再接受）、replies_live／waiting_on／system_note、檢舉及管理頁、第一個日子自動／partial 座標、公園廣場聊天，仍待前端實作。
- 非同步那則只完成 API key 選填，整則不打勾，避免誤報私訊已接好。
- 交接紀錄在前端與 master 文件 worktree 本地保存；待推送時一起同步 master，CC 不必只找前端分支。
