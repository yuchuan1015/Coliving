# 相簿與動態相框發布

2026-09-09。使用者批准「推送 部署 然後git pull看留言板」，本次已完成。

## 正式版本與範圍

- 正式站：https://therookery.space/ 。相簿 /home/photos、鏡子 /agent/edit、名錄 /residents。
- 已部署程式：`55abecc3c753eea08eee61a1c76fbce997889e5d`，前端分支 codex/ui-chrome。包含本地三筆 83f8f52、8097569、55abecc；後續提交只記錄文件。
- 名錄有公開私訊碼才顯示及複製，鏡子可調 dm_code_public；關閉僅隱藏名錄，不讓已知碼失效。
- 鏡子新增「給室友的話」note_to_agent、1000 字、與資料共用保存，部分成功不重傳已成功步驟。
- 相框改為照片相簿：20 張／同時展示 1 張、caption 200 字、multipart 上傳、切換／留空／編輯說明／確認刪除，取後端選擇為準；取代旧文字條目 UI。
- 記憶區桌上的實體框內也跟 photo_frame.photo 換圖，保留原金屬外框、三區底圖、錨點位置、三卡版面與 FAB。返回艙室／回到前景會重新同步，不新增 API 輪詢。
- CSS：`assets/index-5iBXcWMP.css`；JS：`assets/index-Cvj4dJ_8.js`。開發專用 scripts/album-preview 不進 dist，沒把 mock 當正式資料。

## 驗證

- 發布前重跑 170 合約＋5 配色＝175 項測試，以及 TypeScript 和 Vite build，通過。主 JS 508.29kB，既有大於 500kB 的分包建議非 build 失敗。
- 正式 HTTPS 42 靜態檔＋31 路由全部 200，Content-Type／SHA256 符合已保存的本地候選產物；核驗時間 `2026-09-09T03:39:00.799Z`。
- 18 個公開／未登入診斷符合預期：providers、OAuth discovery 回 200；相簿、家具總覽、名錄、使用者、鑰匙、授權、私訊、管理檢舉與受限場域聊天回 401；/mcp 有 resource_metadata challenge。
- 這是靜態發布和未登入診斷，不等於真實帳號端到端驗收。此輪未上傳／刪除真人照片、改留言／公開狀態、寄訊息或使用真實金鑰，未做 Safari 真機 HEIC 選圖測試。本地 mock 的換圖／清空／實體框連動驗證見開發紀錄。

## 備份與部署

- 僅寫 `/opt/coliving/frontend/dist`；backend、nginx、DB 和服務都沒動，也沒有重啟。
- 完整備份 84 檔；本地逐檔 SHA256、checksum rsync 乾跑、上傳前再次遠端快照，全數一致。
- 先上資產，index.html／sw.js 先用同目錄暫存名；新42檔＋舊檔及原入口全部校驗後，才各自原子切換入口，`chmod -R 755 /opt/coliving/frontend/dist`。
- 沒有 --delete；保留44份舊資產／預覽，線上共86檔雜湊全符；檔案與目錄均755，沒有 symlink 或殘留暫存入口。
- 新 index SHA256：`274b069385da06bc279042c3863dbe7e6018a130b076a18826faf3b05d85ecd1`。
- 新 sw SHA256：`a2269be784e8a66153f18c5482c8c7712ffce4117b98566515f9e9405e95178b`。
- 舊 index SHA256：`2c1c65de4a90ccb5490bbe24774c027556f645f35aa7ddd24c0d8d24e15cf55f`。
- 舊 sw SHA256：`22ce4494affc137bf6c24477b0e899fd0dc12833646b0cd3732aaeec70881423`。
- 啟用時間：`2026-09-09T03:38:06.117Z`。
- 本地備份：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/before-album-55abecc-G0QKpl/dist/`；同層保留 candidate、before／staged／after.sha256 和 deployment.json。
- 公開核驗報告：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/album-55abecc-verification.json`。這些備份與報告不上 GitHub；還原前應先確認沒有後續發布，不直接蓋掉更新版本。

## 留言板

- 部署後在獨立 docs worktree 執行 `git pull --no-rebase --no-edit origin master`，顯示 Already up to date；CC 最新仍為 `2159c63`，沒有新留言。上輪列的五项待辦本次都已發布。
- 日記／抽屜維持兩個入口，migration 017 的既有契約不變，未另做介面改版。城市修正與 OAuth 等以前的功能不變。
- 新前端不再呼叫舊 /home/furniture/photo-frame 文字端點，但 **舊客戶端相容期限仍須另確認，本次不批准後端刪除端點**。
- 發布文件與留言另同步 master；程式留 codex/ui-chrome，不能用 master 舊前端重新 build 覆蓋正式站。
