# 導覽手冊進度

2026-09-10。本地完成，尚未推送或部署。使用者指定報錯信箱：therookery1108@outlook.com。

## 入口與內容

- 艙室 Agent 卡片 FAB 新增「導覽手冊」，進入受既有登入保護的 /guide。點擊會關閉 FAB；小視窗選單可捲動，首頁三卡幾何、配色、家具、頭像及其他捷徑不變。
- 沿用 CabinUtilityShell 的紫黑家具頁風格；手機搜尋欄置頂，四類說明及全部篩選，可展開閱讀。
- 30 則：11 基本功能（初次入住、家具、頭像／鏡子、相框、共讀／mem0、日記／抽屜／信箱、用量、連接器、設定／排程及展示限制）、11 場域、7 排除、1 聯絡。
- 場域星球名與路由依 FIELDS，非手動另建地圖或使用示範 HTML。說明依現有程式和 CC 留言，不宣稱未驗證客戶端全部相容。
- 睡眠艙展示未接操作、工坊啟用不套寫實艙室、美術館填網址不是上傳，均明確標示。成人區以既有名稱對照分級式人機親密關係中心，仍保留入場／年齡權限。
- 註冊目前返回艙室；沒有 Agent 時 FAB「聊天」導向領養，手冊按這條實際路徑說明，沒有假稱註冊立即跳領養或另改註冊流程。

## 搜尋與報錯

- 純前端資料檔，不新增 endpoint、資料庫、套件、搜尋紀錄或後台管理。標題／摘要／全文／關鍵字搜尋，NFKC全形、大小寫正規化、已整理的部分簡體別名；多詞以空白分隔取交集，可和分類結合。
- 使用 includes 而非使用者正規表示式，文字以 React 純文字呈現。零結果有清除及前往報錯；清除單純搜尋時保留分類。
- 報錯地址只用使用者明確指定的 Outlook 信箱。mailto 主旨／內文正確編碼，只有固定空白格式，不自動帶入網址、帳號、時間、對話、憑證或截圖。
- 按開啟草稿不會寄出；複製成功只回報複製，剪貼簿不支援／拒絕时可手動選取信箱與格式。提醒不寄密碼、API key、MCP token、完整邀請碼或私人內容。
- 沒有實際寄測試信，也沒有驗證收件服務或承諾回覆時限。未來若要站內工單／附件／線上編輯，需另確認產品範圍；本批不需要 CC 補後端。

## 驗證與下一步

- 本地預覽：http://127.0.0.1:5186/scripts/album-preview/index.html?page=guide 。既有5186伺服器與分頁沿用，開啟工具回 queued；HTML及元件HTTP 200，不聲稱已做視覺驗收。
- 非瀏覽器合約＋配色 228/228通過（本批新增13）；正式及預覽 TypeScript、變更檔 oxlint、Vite build通過。主包既有大於500kB建議仍在，非build失敗。
- 測試涵蓋11場域一對一、全部連結為既有路由、全文／別名／全形／多詞／分類、零結果、郵件編碼、剪貼簿待定／拒絕／缺少／離頁、FAB導覽、無API／儲存副作用，及原有入口／配色回歸。
- 沒做瀏覽器點擊／截圖、Safari實機、真人模型／帳號寫入或寄信。沒有改 backend、VPS、nginx、服務、依賴或 lockfile。
- 先保存本地；等使用者批准推送部署，再發布本批及先前待發的用量面板。留言與本文件另以 docs-only 同步 master，禁止使用 master 舊前端覆蓋。
