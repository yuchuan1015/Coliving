# 寵物圖庫本地接線（2026-09-12）

兩批 28 張寵物圖已由前端完成保留原畫的本地去白底，以及透明度／邊緣／縮圖視覺驗收。後端以同一份清單啟用本地 catalog、普通領養與許願到家。這是本地發布包的整合紀錄；未推送、未部署、未操作正式 API 或資料庫，也沒有建立正式寵物或回填舊寵物的 `asset_key`。

## 交付來源

- 前端工作目錄：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/reconcile-ui`，分支 `codex/ui-chrome`。
- 後端工作目錄：`/private/tmp/rookery-pet-integration`，分支 `codex/pet-integration`；本輪建立在 `d34e986`（許願 runtime `bc24650`）之上。
- 權威清單：前端 `frontend/public/assets/pets/catalog.json`；後端 `backend/data/pets/assets.json` 為逐位元組相同副本，SHA256 `1ceca794f8b6a9ae48907e82dc8dc084cf3acb5c20ea404d8cdefe7910a4e8a4`。
- 前端驗收紀錄：`docs/pet-assets-v1-provenance.json`，SHA256 `9a0f1f383c443e4c629b1b6ec5cd74c23956b78f0cd59cd686b9d1b8dea14b24`。其中每項 `web` 記錄網站用圖的實際 SHA、尺寸、透明度、視覺驗收與原圖保留資訊。

清單版本 `rookery-pets-v1-20260912`，28 個唯一 key 均為 `{id}-v1`，包括 `blue-whale-v1` 與 `honeybee-v1`。網站圖位於前端 `frontend/public/assets/pets/v1/{id}.png`，對應同站 URL `/assets/pets/v1/{id}.png`。每張 384×384、8-bit RGBA PNG，合計 4,136,329 bytes。

`published: true` 僅代表此本地發布包認可該資產可被新領養／到家引用，不代表正式站已上線。圖片仍由前端提供，後端不複製圖檔或建立圖片服務。

## 驗證

主 agent 已逐項核對權威清單與驗收紀錄的 SHA，28 個 key／URL 唯一；逐一讀取前端網站圖，確認其 SHA 與 provenance 完全一致，PNG header 為 384×384 RGBA，總 bytes 相同。本輪未修改或重新生成圖片。

子 agent 新增 `backend/tests/test_pet_published_catalog.py`，以隔離 SQLite、合成帳號及 FastAPI TestClient 驗證真實登錄的 28 筆 catalog、藍鯨普通領養、蜜蜂許願準備／到家／重送，以及未知 key 拒絕且保留原預約，4/4 通過。主 agent 審閱測試，並將此測試與可信登錄邊界測試合跑 10/10 通過，確認 fixture 的還原不影響空圖庫及非法登錄驗證。

既有寵物相關回歸 74/74 通過（4.111 秒），涵蓋可信登錄、許願、邊界、身分競態、信用名額、MCP 與照顧。本輪共驗證 78 個不同測試，沒有重跑與本次清單變更無關的完整後端套件。所有測試透過 `backend/run_tests.py` 禁止對外連線、不讀 `.env`。本地日誌：`/private/tmp/rookery-pet-catalog-regression.log`、`/private/tmp/rookery-pet-published-catalog-tests.log`。

## 發布銜接

本輪維持本地。未來取得發布授權後，需先確保前端這 28 個同站靜態圖片 URL 可用，再啟用包含此登錄的後端版本，避免 API 已允許領養卻找不到圖片。舊寵物 `asset_key: null` 和 emoji 回退維持原狀，不猜測回填。
