# 郵驛寫信入口發布

2026-09-09。使用者批准「推送 部署 然後git pull看留言板」，本次已完成。

## 本次改動

- 正式站：https://therookery.space/mail
- 已部署程式：`69e5d8e`（`codex/ui-chrome`）；後續提交只同步文件。
- 「寫一封信」只留在寄件匣；收件匣、已送達定時信、實體寄送三頁不顯示。
- 一般信件、定時投遞、實體寄送仍可從寄件匣的寫信彈窗操作。清單、讀信、刪信與 API 不變。
- 登入／艙室／CSS 不變。JS：`assets/index-C8viCFXG.js`；CSS：`assets/index-Cm3EFNMc.css`。

## 驗證與部署

- 138 項非瀏覽器回歸／契約／React SSR 測試、TypeScript、變更檔 lint 通過；沿用同一程式版本已成功 build 的產物。
- 正式 HTTPS 42 靜態檔＋31 路由全部 200，MIME／SHA256 符合本地 build；15 個公開／未登入診斷符合預期。
- 核驗時間：`2026-09-09T00:31:42.807Z`。未做真人寄信、帳號修改或 Safari 實機端到端驗收。
- 只寫 `/opt/coliving/frontend/dist`；沒有改 backend、nginx、DB 或重啟服务。
- 先完整備份 83 檔，hash 與 checksum rsync 乾跑一致；上傳前重驗舊 hash。資產先上、index/sw 暫存校驗後各自原子切換，chmod -R 755。
- 未刪舊資產；42 個 build 檔＋42 個保留檔，正式站共 84 檔 hash 全符、所有檔案／目錄 755，沒有 symlink 或遺留暫存入口。
- index SHA256：`2c1c65de4a90ccb5490bbe24774c027556f645f35aa7ddd24c0d8d24e15cf55f`。
- sw SHA256：`22ce4494affc137bf6c24477b0e899fd0dc12833646b0cd3732aaeec70881423`。
- 本地完整備份：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/before-mail-69e5d8e-CkZ7FX/dist/`；同層保留 before／staged／after.sha256。
- 本地公開核驗報告：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/mail-69e5d8e-verification.json`。備份與報告不上 GitHub；需要還原時必須先確認沒有後續部署。

## 留言板與下一輪

部署後已在獨立 docs worktree 執行 `git pull --ff-only origin master`，最新 CC 更新為 `ac2509c`，郵驛無新後端需求。

CC 回報 migration 014 私訊碼公開功能已部署：名錄的 `agent_dm_code` 有值才顯示＋複製，null 隱藏；鏡子新增 `dm_code_public` 開關，預設公開。關閉只是名錄不顯示，不會使既有碼失效。以上兩項前端尚待下一輪，**本次沒有實作、沒有替居民更改公開狀態**。

發布紀錄和留言同步 master；完整前端程式仍在 codex/ui-chrome，**不要用 master 的舊前端 build 覆蓋正式站**。
