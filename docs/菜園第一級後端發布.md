# 菜園第一級後端發布 · 2026-09-11

## 目前停留狀態

**後端程式與資料表已正式發布，API、兩個 MCP、原 wake timer 均正常；新 garden timer 尚未安裝，世界／epoch／首輪投票尚未建立，前端農田入口尚未切換。** 最後核驗為 `2026-09-10T19:35:03Z`（台北 2026-09-11 03:35）。

使用者在「鴉巢前端」得知需要備份、更新後端與資料庫、短暫重啟後，於 `2026-09-10T19:14:20Z` 回覆「一起部署」。後端任務單一負責本次 backend／DB／服務，未修改 frontend/dist、Sites、DNS 或 SSH 安全設定。

自動核准審查仍拒絕「新增且持久啟用每分鐘 garden timer、建立世界與首輪投票」，理由是未認定原授權明確涵蓋這些持久排程與初始化副作用。被拒絕命令沒有執行，前端主線統一補確認；不得以登入 GET、直接 garden_tick、直接寫 DB 或換管道完成同樣初始化。原 wake timer 只是本次維護暫停的既有排程，已依原 active/enabled 狀態恢復。

## 版本與正式環境

- 固定來源 `146f26e6a0624e375d5307921f2aa8401018ea20`，已正常推送 `origin/codex/garden-tier1`。實作 commit 為 `7e073ec`，146f26e 只補入口文件。
- 已核實正式目標 `root@149.28.148.65`：網域解析、nginx server_name 與原正式 backend 相符；沒有使用指向另一 IP 的 `keke` alias。
- 正式程式／WorkingDirectory：`/opt/coliving/backend`，DB 為其下 `coliving.db`。`.env` 與冷備份逐位元相同；uploads、private_photos、memdata、`/root/.mem0` 原路徑保留。
- 候選根目錄：`/opt/coliving-releases/backend-146f26e-20260910T1916Z`。其下 `venv` 已成為正式 runtime，**不得視為暫存目錄刪除**；保存 `candidate-lock.txt`、`candidate-versions.json`、manifest、測試、發布腳本及待啟用的 garden 單位。
- 舊正式 venv 有八組重複 dist-info 版本紀錄，因此保留原 venv，直接使用完整 Linux 測試通過的乾淨 runtime。API、兩 MCP、wake service 各新增 `20-backend-runtime.conf`，只改 ExecStart，WorkingDirectory、環境與其他 unit 設定不變。
- 實際服務：`coliving-api.service`、`coliving-mcp.service`、`coliving-mcp-private.service`、`coliving-wake.service`／`.timer`。API 保持 127.0.0.1:8000 單 worker，兩 MCP 為 8001／8002。

原有 204 個正式程式／requirements 檔全與已知基底或候選吻合，沒有未交接 hotfix。候選 tar SHA256 為 `5c0a98fc5a8221cde2e1b729907b01af19685040d369e8f64046b7580ce9eb1c`；232 檔排除 `.env.example` 後正式寫入 231 檔，全部 SHA256 相符，無整包 rsync --delete 或資料清除。

## 驗證結果

Linux Python 3.12.3 候選 `pip check` 通過，**226 項測試全部通過，146.521 秒**。runner 隔離 DB／uploads／照片／mem0、忽略正式環境檔並封鎖模型網路連線。依賴解析處理了 google-genai／websockets 及 httpx2／httpcore2 相容要求；最終為 google-genai 1.75.0、websockets 16.1.1、httpx2/httpcore2 2.12.0、MCP 2.0.0、mem0ai 2.0.20。完整 78 項版本見 release 目錄 lock，正式直接使用同一個已測 runtime，未於停機時浮動解析套件。

先停 wake 新觸發並等在途完成，再停兩 MCP、API，確認 DB／記憶路徑沒有開啟中的檔案持有者後備份。022／023 在額外 DB 副本各跑兩次，原有 49 張表筆數保留、auth_version 非空預設正確、七張 garden 表為空。正式遷移再次通過原表筆數及 integrity_check。

API→兩 MCP 於 `2026-09-10T19:31:44Z` 開始依序啟動。正式驗證：

- `/api/health` 200；未登入 users/me 及 garden public/private/inventory/progress 均 401。
- 兩項 OAuth discovery 200；公開 `/mcp`、私人 `/mcp-auth` 無憑證均 401 且有 challenge。
- OpenAPI 五條 garden 路由、七張 garden 表存在、DB integrity_check=ok、garden_world 筆數 0。
- 三程序 active、NRestarts=0；本次啟動日誌未見匯入、SQL 欄位、port 或 Qdrant lock 錯誤類型。
- 原 wake timer 恢復後於 `2026-09-10T19:35:00Z` 自然觸發並以狀態 0 結束，未手動跑會發 webhook 的 scheduler 作診斷。

未借住戶帳號／模型 key，未讀住戶內容、發測試訊息或執行玩家動作。上述結果不代替真登入、模型 provider 或農田端到端操作驗收。既有私人聊天及公開 AI 私訊仍可能直接呼叫 mem0／Qdrant，不能宣稱所有 MCP 路徑都已代理 internal API；本次未呼叫那些有副作用的工具。

## 備份與證據

正式冷備份：`/var/backups/coliving/backend-146f26e-20260910T1916Z/`，0700 目錄／077 umask，留在 VPS 私有位置。包含完整 backend（設定、舊 venv、DB、照片、記憶）、獨立 SQLite backup、root mem0 SDK、原 systemd 單位與 active 狀態；backend tar 清單核驗 15,694 項。

| 備份 | SHA256 |
|---|---|
| backend-before.tar | b3ab085254f979c2136095ab48bf9a9aeca6a9796f6429e06a50c79f78f20e2b |
| database.sqlite | 11bc5548f1a1ba2253e52fca296cc3d10e6d7a37878d410afe67b85d7bec1dea |
| mem0-sdk-before.tar | 22b12291bd0e9ed54f5ec2f1c2c41563172f37e8b0949e6b79904a9f0fe39cd3 |
| units-before.tar | be56a074774d552c128b76b57f49633c4f69c2b8e44a39b8eba48444f533c56c |

備份目錄另有完整雜湊、原表計數、副本演練及正式 migration 報告；release 目錄保存 `candidate-tests.log`、`pre-garden-smoke.json`、`release-awaiting-garden-activation.json`。本機無敏感內容的候選及報告在 `/private/tmp/rookery-release-146f26e/`，未下載正式資料備份。

## 接續與回復

待使用者明確同意每分鐘結算與世界／首輪 12 小時投票初始化後，先核對正式 SHA、unit、world 狀態是否有後續變動，再將相同 garden service／timer 交審查。兩份單位已通過 systemd-analyze verify；獲准後才安裝、首次結算、啟用及觀察自然觸發，核驗唯一 epoch、12 候選、投票時限、服務健康，最後通知前端切入口。

現行後端已重新對外服務，不得以部署前 DB 整份覆蓋或退回新認證保護。保留 auth_version、最新密碼與憑證撤銷、新寫入內容和 garden 表；舊 runtime 雖仍完整，切回前仍需驗證與新程式相容。

還原 units-before.tar 不會刪除四個新增 runtime drop-in，必須明確處理再 daemon-reload 才會恢復原 ExecStart。未來若已安裝 garden 單位，回復前先停用它們再處理新增檔案；目前這兩個單位尚未安裝。其他資料／認證回復規則沿用 [後端修復部署準備](後端修復部署準備.md)。
