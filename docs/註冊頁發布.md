# 邀請碼註冊頁 · 新域名發布紀錄

## 已部署 · 2026-09-08

使用者批准「ok 那推送然後部署」。正式入口：[邀請碼註冊](https://therookery.space/register)。
程式已推至 `codex/ui-chrome`；部署源碼為 `55e12fac744645cb1f6a139cb750600d29445f34`。
此文件和部署留言另同步 master，不把 master 的舊前端拿去部署。

## 本次內容

- `62c00ee`：註冊頁沿用登入頁既有紫色星空／卡片，樣式限定 registration，不改登入、艙室、十一場域配色。
- 邀請碼、帳號、選填暱稱、密碼、必填出生年及自動 timezone 的原註冊流程保留。出生年不能更改的提醒、錯誤保留輸入、防重送仍在。
- 補標籤關聯、手機安全區、16px 輸入文字；不自動叫出鍵盤，長表單自然捲動。
- `55e12fa`：field-preview 返回正式站改 https://therookery.space/，接口文件目前網址同步；API 仍用相對 `/api`。
- 收到 CC 的域名遷移 a3971a4 和自寄定時信修復 6f5741f；後端由 CC 部署，本輪未改後端。歷史部署紀錄中的舊域名保留作當時狀態，後續正式連結用新域名。

## 驗證

- 43 項非瀏覽器契約／React server render 測試全通過，TypeScript 與 Vite production build 通過。
- 新域名 42 個靜態檔及 16 個正式路由均 HTTPS 200，MIME／SHA256 與本地 dist 一致。
- `GET /api/agents/providers` 200 JSON；未登入 `GET /api/users/me` 401 JSON。
- 舊註冊網址回 301 到 https://therookery.space/register。
- 未使用真實邀請碼、未建立帳號、未執行 Safari 實機或真實 API 寫入驗收；以上不等於完整端到端驗收。

## 部署與備份

只更新 `/opt/coliving/frontend/dist`，最後 chmod -R 755；沒有改 backend／nginx，沒有重啟服務。
完整舊版備份與 checksum dry-run 無差異；保留線上舊雜湊資產，不清空目錄。
先上資產、暫存 index/sw，42 個新檔與兩個舊版核心雜湊均核對後，再逐檔原子切換。

- 備份：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/before-register-space-n7avus/`
- 驗證報告：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/register-55e12fa-verification.json`
- 舊 index：`cc3d5df5a666784d1d0e558e497a69f39e7da32a27dc91b35023459992eaaa15`
- 舊 sw：`2fc2b0c3affc12ce785e7b216abb6b0677b99f14d886630e4696edc720cf4612`
- 新 JS：`assets/index-DbD6SWBm.js`
- 新 CSS：`assets/index-qpPinwjk.css`
- 新 index：`4df720b3487203501397e1d59a28e019e87491b066bcf8d7b0412eaa60d72a96`
- 新 sw：`7a6c8ee667a6ad08605fd657545f051aa96edf8b27036e8f31797f532af6662d`

若需回滾，先核對現行版本，再從上述完整備份恢復前端；仍採資產先回、index/sw 最後切換，不能刪空 dist 或動後端。
PWA 安裝綁定 origin，舊域名安裝的需由新域名重新加入主畫面；舊登入狀態不會自動搬到另一個 origin。
