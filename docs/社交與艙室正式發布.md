# 社交與艙室正式發布

2026-09-09。使用者批准「推送！部署！」，以下已完成，不是本地預覽。

## 正式版本

- 正式站：https://therookery.space/
- 部署程式：`b423850`，發布前 `codex/ui-chrome` 的 `0dbea69` 已推 GitHub。包含先前 `75ceb68` 的社交／艙室操作，以及最新成人區／健康中心聊天；後續文件提交不改這份前端程式。
- 私訊碼、等待狀態／有限輪詢、檢舉與管理審核；九區聊天在場名單／@／到期／Markdown匯出；座標與名錄；外部記憶／409相框引導；衣櫃、餐桌、寵物、時區與對外大腦設定均隨本版上線。
- 成人區仍需確認進入，聊天的實際授權交給後端 `550354e`；健康聊天與文章分級分開。讀取／發送／匯出403顯示後端原文，不再顯示聊天內容與操作。
- CSS／JS：`assets/index-Cm3EFNMc.css`、`assets/index-CChkLiCs.js`。登入配色、艙室圖片／幾何未改；本地測試fixture不包含在dist。
- 本文件與發布留言同步master，程式仍在前端分支；**不可用master的舊前端打包覆蓋正式站**。

## 核驗

- 部署前重新跑137項非瀏覽器契約／回歸測試、TypeScript及變更檔oxlint，全過；前端來源與上輪已成功build的程式完全一致，沿用相同產物。
- 正式HTTPS：42靜態檔＋31路由全部200，MIME和SHA256一致。包含新增名錄／名片、管理檢舉、排程、兩個受限場域與既有登入／授權頁。
- 15個公開／未登入診斷符合預期：providers與兩個OAuth discovery回200；users/me、鑰匙、授權清單、私訊清單、管理檢舉，以及adult／health的present、chat、export均401 JSON。未帶憑證 /mcp回401及resource_metadata challenge。
- 部署後全部83檔（含41保留檔）hash符合預期，所有檔案／目錄755，沒有symlink或遺留暫存入口。原有SAMEORIGIN／no-referrer／nosniff標頭仍在。
- 上輪內建瀏覽器的假資料功能驗收仍有效；本輪未以真人帳號發訊息、檢舉、改資料、改出生年、產廢鑰匙或做Safari手機／正式API端到端操作驗收。不能把靜態200或未登入401當成真人完整功能驗收。
- 核驗時間：`2026-09-09T00:13:51.763Z`。

## 備份與部署範圍

- 只寫 `/opt/coliving/frontend/dist`；沒有改backend、nginx、DB或重啟任何服務。
- 先完整備份81檔，逐檔hash與checksum rsync乾跑一致。上傳前再次核對全部舊hash；先上資產、入口暫存，在42新檔＋41保留檔＋原index/sw守衛全部通過後，才各自原子切換index/sw，chmod -R 755。
- 沒用rsync --delete，保留舊雜湊資產與8份手機配色預覽。正式目錄現在83檔。
- 新index SHA256：`7c16424647782123394ef72e74c04f323f0883e03fab3a750bd232336820872c`。
- 新sw SHA256：`bf47d24771414b29b400b4825908bc222dcf519140f528b8cee873c8aaa48dd3`。
- 原index SHA256：`59717f43b6ee25499b75355845ad7fe17a54dc636654448352d38dac54559643`。
- 原sw SHA256：`ec9fb496808e3ebeb094b2d54ccde32196617e09b94c23e167da5ce23501aa7d`。
- 完整備份：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/before-social-b423850-MpYGnY/dist/`；同層before/staged/after.sha256保存核驗依據。
- 公開核驗報告：`/Users/linyuchuan/Documents/Codex/2026-09-04/hi/work/cenyu/ya-chao/deploy-backups/social-b423850-verification.json`。
- 備份與完整hash報告只保存在本地，不上GitHub。若日後需回復，先確認沒有其他人的後續發布，再使用此備份恢復入口；不可直接覆蓋更新的部署。

CC的年齡閘交接已接完，沒有本次必須補的新後端需求。其他CLI／網頁客戶端真人OAuth兼容驗收與舊管理時間線bed_label等零星項目，不在此次完成聲明內。
