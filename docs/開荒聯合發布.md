# 開荒聯合發布 · 2026-09-11

## 當前狀態：後端已更新，農田啟用與前端切換待明確確認

使用者在得知需備份、更新後端與資料庫、短暫重啟後，回覆「一起部署」。前後端分開發布，後端任務單獨處理正式 backend、資料庫與服務；前端任務不交叉操作後端。

### 前端

- 已提交並推送 `codex/ui-chrome`：`63d69a5e1637706097a9de97150196fd113d1116`。
- 使用者核定的公共場域藍底與綠色點綴保持不變。`/outside → /frontier → /frontier/garden`；03 Procyon 在廣場下方，魚池／牧場／市場仍為規劃中。
- 360 契約測試、10 艙室配色測試、TypeScript、1941 則繁簡字典、農田範圍 lint、正式 Vite/PWA build 通過。
- 候選 CSS `assets/index-9h3p3rpv.css`、JS `assets/index-Cpyal6FE.js`。44 個正式檔案；相對現場只有 index/sw 變更及4個新增資產，沒有其他非雜湊資產覆蓋。
- 維護前完整備份 `../deploy-backups/pre-maintenance-63d69a5-V7Fki7/`（相對本 worktree），110 檔 rsync checksum dry-run 一致。原 index SHA-256 `3e7e5ea9d99476fd4b9514cc1d5e22e88a7daf083a64c2d7659575b33e69d493`，原 sw SHA-256 `e577cc37d5301222d048b6d3a83ffa8b912487a03ae88522332696076f641228`。
- 已補發布驗證工具的兩個農田路由、JPEG MIME 與匿名 `/api/garden/public` 應為401。隔離模擬驗證能正確拒絕JPEG錯誤MIME和garden404；預計正式查44資產、37路由、23項診斷。
- **正式前端未上傳／切換。** 上述工具模擬不是正式發布驗收，沒有操作住戶帳號或測試付費模型。

### 後端已完成（由後端任務直接交付）

- 已核實 `149.28.148.65` 的網域、nginx 與服務是 `therookery.space`，不使用另指其他IP的 keke alias。
- 程式 `146f26e6a0624e375d5307921f2aa8401018ea20` 已推送 `origin/codex/garden-tier1` 並發布。Linux 乾淨環境 pip check、226項完整測試通過。
- 備份在 VPS `/var/backups/coliving/backend-146f26e-20260910T1916Z/`；022／023先在副本連跑兩次，再套用正式DB，49張原表的筆數保留，完整性檢查通過。
- API與公開／私人MCP已恢復；沿用 `/opt/coliving/backend` 工作目錄與既有設定、資料路徑，執行環境使用 `/opt/coliving-releases/backend-146f26e-20260910T1916Z/venv`，原venv保留。
- 10項未登入HTTPS診斷、5條農田OpenAPI路由、7張資料表檢查通過；`/api/garden/public`從404變為401，未登入仍不可讀取。

### 尚未啟用，不可用其他管道繞過

自動核准審查拒絕安裝／持久啟用每分鐘 `coliving-garden.timer`，以及首次執行 `garden_tick` 建立 world／epoch／第一輪12小時投票，要求使用者明確同意這些持久副作用。

截至此紀錄，新農田unit尚未安裝，world與投票未建立。不能藉由已登入GET、直接寫DB或其他執行路徑達成同樣初始化。前端入口保持原版，等待使用者確認後，再由同一後端任務完成啟用、交付ready，前端才按即時備份與雜湊guard流程切換並執行正式公開檢查。

不因等待啟用而回退新認證／隱私保護，不回復舊DB或改JWT_SECRET。既有wake排程僅因維護暫停，由後端按原啟用狀態恢復；其最後結果以後端發布紀錄為準。
