# 艙室向登入頁對齊 · 本地待確認

2026-09-09。使用者明確指定「主要想要和登入那個頁面對齊，因為現在的登入頁是我比較滿意的」。同意先做本地對照，不推送、不部署。

## 視覺基準

- 登入頁原樣不動。艙室引用既有 --ya-ink / --ya-accent / --ya-muted / --ya-line：冷白 #f4f0ff、淡紫 #c9a7ff、輔助紫灰 #a9a1c7。
- 背景改登入同款 #020207；上下卡片用登入的 rgba(8,6,24,.82) 玻璃底，低強度方向光、內緣反光和陰影增加層次，不再用灰紅紫實色。
- 家具錨點、滑塊、FAB、名牌及既有彈窗同步同一組冷紫色票。沒有新增按鈕、字句或改變操作方式。
- 圖片只加 CSS brightness(1.32) contrast(.96) saturate(.96)，三區共用。没有重生或修改圖片檔，不改裁切、尺寸、家具構圖或錨點座標。濾鏡只在 img，不加在包含可點擊元素的 scene。
- 卡片本身不加 backdrop-filter，避免其 stacking context 將 FAB 困在 sibling dismiss 層下面；保留原 z-index 與操作層級。

## 不變的部分

- 手機三卡排列、上下固定高度／中間伸展、safe-area、圓角、間距、字級。
- 頭像與 FAB 圓心對齊、FAB 直徑為頭像一半、滑塊和家具的全部位置。
- 登入／註冊、三區圖片、首頁 TSX、鏡子唯一資料更新入口、路由、API 與 backend。
- 最新使用者基準優先於舊刻度表的藍金四色與直角限制；不為統一舊表而改掉已驗收的版面。

## 本地預覽

- http://127.0.0.1:5173/scripts/cabin-theme-preview/index.html
- 桌面並排原登入／新版艙室；窄螢幕按頂部按鈕切換。艙室仍可切三區、點錨點、展開 FAB／彈窗。
- 使用真正的 LoginPage / HomePage，不另畫一套近似畫面。預覽標明示意資料，透過獨立記憶體 adapter 提供假資料，所有寫入拒絕、不登入、不接正式帳號。
- 預覽只有 scripts 下的獨立開發入口，沒有在正式 App 增加任何免登入路由；production build 不含此入口或 mock。

## 驗證

- 51 項原回歸測試＋5 項配色／版面／圖層／預覽隔離檢查＝56 項通過。
- 正式 TypeScript、預覽 TypeScript、production build 通過。
- 與 3799ef1 比較，非配色的 CSS 宣告一致；首頁、家具資料、圖片、登入樣式、API 和 backend 的 git diff 為空。
- 本地預覽 HTML、TSX 及樣式唯讀請求為 200；未做瀏覽器截圖／互動或 iOS Safari 實機驗收，最終亮度與質感待使用者查看。
- 僅本地保存；正式站維持上一次 c7d3bec 程式版本。批准後才另外推送部署。
