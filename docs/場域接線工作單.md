# 場域接線工作單 · 2026-09-08

使用者批准「直接安排工作然後開始忙」。在獨立 codex/ui-chrome 開發，保留青綠視覺與已發布 field-preview 作對照；本輪先本地實作，不自動推送部署。

## 排程

- [x] 前置：出生年一次性補填、已填唯讀、確認後才保存；補齊註冊必填出生年。
- [x] 共用：正式場域頁框架、登入／錯誤／空狀態、送出防重、社區時間。
- [x] 第一批：AI 私訊、郵驛、公園。
- [x] 第二批：圖書館、美術館、歷史館。
- [x] 第三批：女性健康中心、成人區，遵守後端分級。
- [x] 第四批：工坊、微瀾，遊戲依後端 legal_actions。
- [x] 廣場風格統一、正式出艙導航接線。
- [x] API 契約與非瀏覽器測試、build、保存與 CC 留言。

## 接線原則

使用現有 JWT client 與 /api，不重新做登入或儲存系統。資料只信後端，不把 mock 當 fallback，不自動送信、發起 AI、投稿或遊戲動作；每次寫入由使用者明確操作。刪除、一次性出生年先確認。送出失敗保留輸入，已成功但重讀失敗明示部分成功，避免重複送出。

CC 最新 master c4e5a16：場域 API 已部署；birth_year 補填接口只能一次。工程缺口直接留 docs/工地留言板.md，前端不修改 backend。

## 本輪驗證與下一步

35 項非瀏覽器回呼／API 契約與 React server render 測試通過；build、原 HTML 靜態驗證通過；新增範圍 lint 無警告。詳見 `十一場域正式接線.md`。已保留所有舊 HTML／素材與 backend，沒有連真實帳號做寫入。

- [ ] Safari／手机與實際登入角色／API 聯合驗收（本輪未做）。
- [ ] 使用者批准後推送前端與部署（本輪不自動做）。

工程板三則：本地完成與出生年部署確認、定時郵件追蹤／offset、skin apply flush 疑點。只以文件工作區同步 master，不把前端合入後端 checkout。

保存結果：前端本地 commit `aed01f2`；工程板與接線文件已由獨立 worktree 推到 `master b5854df`。遠端 `codex/ui-chrome` 仍是 `620c83f`，本輪正式前端沒有推送或部署。兩個工作區皆保留，CC 的共用 checkout 未切分支。
